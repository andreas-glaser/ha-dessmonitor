[pytest]
minversion = 8.0
addopts = -ra --strict-markers --durations=10
testpaths =
    tests
asyncio_mode = auto
filterwarnings =
    ignore::DeprecationWarning:aiohttp.*
markers =
    ha_integration: marks tests that spin up a Home Assistant instance (deselect with -m "not ha_integration")
